name: 'opendatahub_dbt'
version: '1.0'
config-version: 2

# Répertoire des modèles
model-paths: ["models"]

# Répertoire pour les tests DBT 
test-paths: ["tests"]

# Répertoire des snapshots 
snapshot-paths: ["snapshots"]

# Répertoire pour les seeds
seed-paths: ["seeds"]

profile: 'opendatahub_dbt'

models:
  opendatahub_dbt:
    bronze:
      +schema: bronze
    silver:
      +schema: silver